A simple page a script to demostrate how to add an image to a web page and update the github pages hosting it.

Run update.py with the signature `python3 add_image.py <path_to_image_file>` in the root folder.
